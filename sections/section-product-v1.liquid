<div data-section-id="{{ section.id }}" data-section-type="section-product-v1" style="{% if section.settings.margin_top != blank %}margin-top: {{section.settings.margin_top}}px;{%endif%} {% if section.settings.margin_bottom != blank %}margin-bottom: {{section.settings.margin_bottom}}px;{% endif %}">
  <div class="section-product-v1 mt-moile">
    <div class="container container-casa">
      
        <div class="text-center title-all-home1">
          {% if section.settings.title_section_product != blank%}
          <h3 class="block-title">{{section.settings.title_section_product}}</h3>      	
          {% endif %}
          {% if section.settings.button != blank %}
          <a class="button"  href="{{section.settings.url_button}}">{{section.settings.button}}</a>
          {% endif %}
        </div>
     
      <div class="row">
        <div class=" engoc-row-equal">    
          {% assign products_limit = section.settings.products_limit %}
          {% assign col = section.settings.select_collection %}
          {% for product in collections[col].products limit: products_limit %}
          <div class=" col-lg-3 col-6 col-md-4">
            {% include 'product-item-v4' %}
          </div>

          {% else %}
          {% for i in (1..12) %}
          <div class=" col-lg-3 col-6 col-md-4">
            {% include 'empty-product-item' %}
          </div>
          {% endfor %}
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>
{% schema %}
{
  "name": "Product v1",
  "settings": [
	{
      "type": "text",
      "id": "title_section_product",
      "label": "Title section Product",
      "default": "Daily Essentials"
    },
	{
      "type": "text",
      "id": "button",
      "label": "button ",
      "default": "Explore all"
    },
    {
      "type": "url",
      "id": "url_button",
      "label": "URL button 1",
      "default": "/collections/all"
    },
	{
      "type": "header",
      "content": "Info Product"
    },
    {
      "type": "collection",
      "id": "select_collection",
      "label": "Select Collection"
    },
	{
      "type": "range",
      "id": "products_limit",
      "label": "Limit products",
      "min": 2,
      "max": 50,
      "step": 1,
      "default": 8
    },
	{
      "type": "header",
      "content": "Info button"
    },
    {
      "type": "header",
      "content": "Section Space"
    },
    {
      "type": "text",
      "id": "margin_top",
      "label": "Margin Top",
      "info": "Defined in pixels. Do not add the 'px' unit."
    },
    {
      "type": "text",
      "id": "margin_bottom",
      "label": "Margin Bottom",
      "info": "Defined in pixels. Do not add the 'px' unit."
    }
  ],
  "presets": [
    {
      "name": "Product v1",
      "category": "HOME 1"
    }
  ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}